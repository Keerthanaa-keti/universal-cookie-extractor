__pycache__/
*.pyc
output/

# OS junk
.DS_Store
**/.DS_Store

# large media / test artifacts
*.mp4

# secrets & keys — NEVER commit (Cookie Vault v3)
.env
.env.*
!.env.example
*.key
*.pem
.cookie-vault/
cookie-vault-*.key
secrets.*
supabase/.env
supabase/functions/**/.env

# baked vault defaults (local only; keeps anon key out of the public repo)
vault-config.js
